<!DOCTYPE html>
<html dir="ltr" lang="<%= I18n.locale %>">
  <head>
    <meta charset="utf-8">
    <title>Stellar Watcher: Receive notifications every time your wallet changes.</title>
    <meta http-equiv="x-dns-prefetch-control" content="off">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <%= render "favicon" %>

    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => "reload" %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => "reload" %>
  </head>

  <body data-route="application/controllers/<%= controller.controller_name %>/<%= controller.action_name %>">

    <div class="container">
      <%= render "header" %>
      <%= render "flash_messages" %>

      <div class="content">
        <%= yield %>
      </div>
    </div>

    <%= render "footer" %>
  </body>
</html>
